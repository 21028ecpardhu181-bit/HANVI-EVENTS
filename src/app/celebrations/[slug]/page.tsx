import ServiceDetailPage, { generateMetadata as generateServiceMetadata, generateStaticParams } from '@/app/services/[slug]/page';

export { generateServiceMetadata as generateMetadata, generateStaticParams };
export default ServiceDetailPage;
